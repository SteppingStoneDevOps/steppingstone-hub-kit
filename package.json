{
  "name": "@steppingstone/hub-kit",
  "version": "0.5.0",
  "description": "Shared design-system primitives for the SteppingStone hubs (advisor-hub, admin-hub, talent-hub). Single source of truth for the UI kit — consumed as source via transpilePackages + @source.",
  "private": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "peerDependencies": {
    "lucide-react": ">=1.0.0",
    "next": ">=16",
    "react": "^19",
    "react-dom": "^19"
  }
}
